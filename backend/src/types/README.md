# Types Layer

Contains TypeScript type definitions used across the application.

## Includes

- Request input types (RegisterInput, LoginInput)
- JWT payload type (UserPayload)
- Auth response types (AuthTokens)
- Custom error types (ApiError)

## Purpose

- Ensure type safety across services and controllers
- Avoid duplication of type definitions
- Improve code readability and maintainability